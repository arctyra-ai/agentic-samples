# No server-side API keys required.
# All API keys are provided by the user at runtime and never stored server-side.
# This file exists as a placeholder for any future environment configuration.

# Optional: Override default request timeout (milliseconds)
# REQUEST_TIMEOUT_MS=30000

# Optional: Override default max response size (bytes)
# MAX_RESPONSE_SIZE=10485760